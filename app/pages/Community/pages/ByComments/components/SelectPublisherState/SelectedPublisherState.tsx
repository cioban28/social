import React from 'react';

const SelectedPublisherState = ({}) => (
  <div style={{ textAlign: 'center', textTransform: 'uppercase', margin: '30px 0', opacity: 0.6 }}>
    <h2>🏆</h2>
    <h3>Please select site to see users</h3>
  </div>
);

export default SelectedPublisherState;
